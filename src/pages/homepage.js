import { About } from "../components/about";
import { Editing } from "../components/edit";
export const HomePage = () =>{
    return(
        <main>
            <About/>
            <Editing/>
        </main>
    );
}